memory:
  private:
    transaction_list:
      type: any
      mappings:
        - type: controlled

    transactions_display:
      type: text
      mappings:
        - type: controlled

    txn_list_card_last_four:
      type: text
      mappings:
        - type: from_llm

    txn_list_filters:
      type: text
      mappings:
        - type: from_llm

    selected_transactions:
      type: any
      mappings:
        - type: from_llm
