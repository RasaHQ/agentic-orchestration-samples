version: "3.1"

slots:
  return_value:
    type: any
    mappings:
      - type: controlled

  confirmation:
    type: bool
    mappings:
      - type: from_llm

responses:
  utter_agent_busy:
    - text: "I'm still working on that request. Please wait a moment."
