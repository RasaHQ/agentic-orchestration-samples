version: "3.1"

slots:
  cards_list:
    type: text
    mappings:
      - type: from_llm
    scope: list_cards

  cards_raw:
    type: text
    mappings:
      - type: from_llm
    scope: list_cards

responses:
  utter_cards:
    - text: "You currently have the following cards: \n\n{cards_list}"

actions:
  - action_list_cards
