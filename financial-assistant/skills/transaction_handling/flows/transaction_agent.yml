flows:
  transaction_agent:
    description: |
      Handles disputing credit card transactions. Accepts transaction IDs from users
      and marks them as disputed for fraud investigation.
    agent:
      protocol: rasa
      business_logic_prompt: ./skills/transaction_handling/prompts/prompt_template.jinja2
      module: project.custom_components.transaction_agent.TransactionAgent
