memory:
  private:
    filter_transactions_card_last_four:
      type: text
      mappings:
        - type: from_llm
      scope: filter_transactions

    filter_transactions_filters:
      type: text
      mappings:
        - type: from_llm
      scope: filter_transactions

    selected_transactions:
      type: any
      mappings:
        - type: from_llm
      scope: filter_transactions